# 更新日志

## [未发布]

### 新增
- 统一表冠旋转振感策略层：`CrownScrollHelper` 新增 `dispatchAdjustTick` 调速档位微振（自动滚屏调速等场景），按物理档位（12 单位）累积计振，与滚动页 24px 门限微振、无内容页空转档位微振并列，构成覆盖全部页面的三类统一表冠振感节奏；新增 `ListView.bindCrownScroll` 扩展统一安装原生列表表冠管线。
- 添加了无线传书 Web 端书籍导出下载支持（`GET /api/download`），支持网页端一键下载导出手表内已存书籍。
- 添加了点按整屏翻页支持：支持上下分屏点按（上半屏上翻、下半屏下翻、腰部呼出菜单）与左右分屏点按，并在翻页时自适应保留 32dp 视觉锚点行以防跳跃眩晕。
- 添加了网文排版智能净化引擎：自动压缩 3 行及以上连续空行，剥离行尾与冗余空白，自然段首自适应规范双全角空格缩进。
- 添加了 AMOLED 纯黑深红夜视色彩主题：基于纯黑底色结合低照度珊瑚红单色显示，大幅降低睡前暗光环境视网膜刺激与能耗。
- 添加了轻量阅读活跃统计器：后台精准记录活跃阅读时长，并在设置菜单中即时呈现累计小时与分钟。
- 添加了表盘边缘弧形快速寻道滑块：专为 466x466 圆形表盘右侧弧边定制的极坐标触控导轨，滑动实时呼出大字悬浮胶囊显示目标章节与百分比，松手即跳。
- 添加了无线传书 Web 端双向书架管理与删书支持：在局域网网页端新增已存书籍实时列表，支持直接删除指定书籍并即时同步手表端。
- 添加了系统衬线字体（宋体）切换支持：设置菜单支持一键切换系统黑体与衬线字体。
- 添加了系统深色模式原生 Window 纯黑启动主题（`values-night/themes.xml`），彻底消除夜间冷启动瞬间白屏爆闪。
- 添加了物理表冠事件焦点丢失兜底路由，杜绝触摸悬浮组件后表冠旋转失灵问题。
- 添加了 Wi-Fi 无线传书高对比度二维码扫码直连支持，手机相机扫描即可直达网页传书端，无需手动输入 IP 与端口。
- 添加了目录界面一键定位当前章快捷功能，支持平滑居中滚动至正在阅读的章节。
- 添加了正文阅读底部微光进度指示，安静阅读时平稳显示章节与百分比，滑动翻页与转动表冠时主动隐去避让。
- 添加了正文两端对齐排版支持，消除圆屏窄宽度下的右侧参差空隙。
- 添加了 RSVP 闪读模式黄金注视焦点（ORP）高亮显示与视线对齐微标。
- 添加了 RSVP 闪读模式左右滑动手势微步回退与快进调节功能。
- 添加了书架书籍删除两段式防误触确认与自动超时恢复机制。

### 修复
- 移除全部界面的触底/触顶边界振感反馈（产品决策"到底以后不反馈"）：表冠滚动与惯性滑动到达边界后仅静默钳制，不再响 301/长按边界振感；同步删除"一次一碰"武装状态机与 `RotaryHapticManager.performBoundaryFeedback`，原厂 301 波形仅保留给传书成功确认反馈。
- 修复了物理表冠无法滚动到章节底部的问题：`dispatchScroll(ScrollView)` 的边界判定最大滚动量漏加了上下 padding（阅读页 `clipToPadding=false` 时滚动范围延伸进 padding 区），边界提前约 108dp 触发，末段滚动被静默钳制并伴随误报边界振感。
- 基于系统桌面 HeyLauncher 字节码逆向，全面对齐表冠滚动手感与震感管线（一份执行层 + 一份策略层）：
  1. 全部马达调用投递至专用 `crown_vibrate` 后台线程（launcher 原样实现）：UI 线程直调 linearmotor 私有服务会因 IPC 阻塞造成震感滞后、输入与页面显示脱节、滚动失去惯性观感，这是历史"震动连成一片"问题的直接根因；齿轮微振 Waveform 302、边界 301（strength 2，与 launcher 字节码一致）恢复为首选波形，View CLOCK_TICK/LONG_PRESS 与标准 Vibrator 仅作非 OPPO 设备兜底；
  2. 统一全部滚动面（阅读页原生 ScrollView / 目录原生 ListView / 菜单与书架 Compose ScrollState）共用同一套惯性管线：速度估计复刻 launcher 公式（自上次停顿起的累计位移 × 700 / 窗口毫秒数，停顿 25ms 清零窗口）与门限（500 起振、±2000 上限）；转动期保持纯 1:1 跟手，最后一次事件 25ms 后仍无新事件且速度达门限才启动 `OverScroller.fling`（launcher 在转动期即时武装 fling，事件间隙 fling 额外贡献位移，实机表现为快速段越滚越快，故改为松冠延迟武装）；惯性期间静默（launcher 惯性段同样无齿轮微振），带速度停在边界静默钳制；章节切换恢复位置前强制终止 fling 防止位置被拖走；
  3. 灵敏度对齐系统：表冠增量 → 像素换算改用系统 `ViewConfiguration.getScaledVerticalScrollFactor()`（实机实测 1.0 px/轴单位，与 HeyLauncher 同源），弃用自标的 64px/档 —— 该标定灵敏度约为系统基准的 5.3 倍，是"转一点滚一大截"的直接原因；24px 齿轮微振门限在此系数下恰好对齐物理档位节奏；
  4. 修复了阅读页自动滚屏表冠调速逐事件连震（仅 20ms 管理器节流兜底）的问题：统一改走 `dispatchAdjustTick` 每物理档位一振，快速转动不再振感粘连；
  5. 移除 RSVP 闪读页被 Activity 顶层管线消费后永远收不到事件的死表冠分支（第二套 ±25 调速参数与配套焦点申请），表冠统一由顶层管线 → `ReaderViewModel.handleRotaryScroll` 单路分发；删除 `CrownScrollHelper.handleRotaryScroll` 无引用兼容别名。
- 根治了多页面表冠旋转无振感与滚动手感错乱的问题（对齐阅读页实现语义）：
  1. 修复了 Compose 页面（菜单 / 书架）表冠分发方向反转缺陷：`ScrollState` 语义为 `value += delta`（正 = 向下翻页），原 `dispatchRawDelta(-pixels)` 与阅读页原生 `smoothScrollBy(+pixels)` 恰好相反，导致页顶顺时针被钳制为完全无响应、逆时针反向滚动；
  2. 修复了表冠事件依赖 1dp 隐形焦点锚点存活的投递缺陷：新增 `CrownScrollTargetRegistry` 页面级注册表，由 Activity 顶层管线在 RSVP 调速分支后直接寻址前台页面，替代 `requestFocus` 焦点游戏，杜绝焦点迁移后表冠事件丢失；
  3. 统一正向瞬时分发：ScrollState 语义 `value += delta`（正 = 向下），与阅读页 `smoothScrollBy(+px)` 同向；线性标定后单事件仅数像素，本机约 60Hz 表冠事件流即为平滑滚动源，移除逐事件弹簧动画（动画取消链会造成距离损失与抖动）；
  4. 新增无线传书页表冠空转反馈（`dispatchIdleTick`）：无滚动内容页面按系统 app 惯例给出齿轮微振，不再完全无响应；
  5. 新增 `CrownScrollDispatchTest` 回归测试锁定方向 / 齿轮微振 / 边界静默钳制语义，并开启单测 `returnDefaultValues` 使含时间源逻辑可在 JVM 测试；
  6. 实机标定表冠单位换算（OWW251 `getevent` 实测）：oplus_crown 上报高分辨率相对滚轮单位流（慢转约 56 事件/s、单事件值 1~5，快甩单事件可达 ±50+），约 12 单位 = 一个物理档位；重写 `normalizeToPixels` 为全量线性换算（每档 64px ≈ 5.33px/单位），废除 |值| ≤ 2.5 档位启发式与像素透传分流 —— 该分流与整机编码不匹配，是历史上调整步进常量无效果、慢转爬行与快甩飞滚的直接原因；
  7. 新增触底/触顶安全边界钳制：到达边界后滚动安全停止，杜绝越界滚动（边界振感及其"一次一碰"状态机已在后续按产品决策整体移除，触底完全静默）。
- 修复了无线传书 Web 端删除书籍时未同步清理 DataStore 书架映射导致幽灵书籍残留的缺陷。
- 修复了弧形寻道滑块缺失触摸滑动位移门限（Touch Slop）导致轻触误跳章的问题。
- 根治了实机阅读进度记忆概率性落后与跳章问题的四大根因：
  1. 修复了 View 尚未完成测量（高度为 0）时执行位置还原导致跳回章首的时序缺陷，引入 `safeRestoreScrollPosition` 与布局就绪监听保障机制；
  2. 修复了垂直像素与字符数线性比例换算导致的漂移缺陷，重构为基于 Android 原生 `TextView.layout` 的行首精确双向映射（`getLineForVertical` 与 `getLineForOffset`），实现字级绝对定位；
  3. 修复了退出或切后台时防抖持久化延迟导致进度被丢弃的缺陷，在 Composable `onDispose` 与 Activity `onPause` 时执行无延迟强制冲刷写入；
  4. 修复了内部 ViewHolder 闭包捕获旧章节内容的缺陷，重构为动态引用 `holder.currentContent`。
- 修复了章节检测器 ParcelFileDescriptor 句柄泄漏引发的 Linux 文件描述符耗尽问题（使用 `AutoCloseInputStream`）。
- 修复了物理表冠滚动方向反向的问题，顺时针向下翻页贴合物理直觉。
- 修复了 ChapterDiskCache 磁盘缓存命中字数虚高 3 倍的问题，升级至 Version 2 协议并精准持久化 `totalChars`。
- 修复了无线传书流式写入由于中断导致坏损书籍残留在私有存储的问题，增加 `totalRead == contentLength` 完整性校验。
- 修复了超大 EPUB 文件解析时解压 Map 占用大量内存导致穿戴设备 OOM 崩溃的问题。
- 修复了目录与书签列表中 AndroidView factory 闭包捕获与数据更新不同步的潜在缺陷，提取显式 Adapter。
- 修复了阅读器左侧边缘滑动调光与正文滚动的双重拔河手势冲突，彻底移除边缘调光手势，全屏触摸 100% 专职归还正文翻页。
- 修复了按住屏幕慢读拖拽超过 1.8 秒时自动滚屏强行复苏导致的画面抖动与手势撕裂问题。
- 修复了阅读器底部进度指示与自动滚屏胶囊在圆表 6 点钟极限收缩区被物理切角断头断尾的问题，边距提升至安全弦长区。
- 修复了目录界面顶部 Tab 胶囊与底部操作栏飞出圆屏表盘外部的切角问题。
- 修复了书架搜索框在手表全屏输入法（Extract Mode）下频繁注销注册监听引发的输入法震荡闪退与字符跳跃问题。
- 修复了手表软键盘弹出遮挡搜索框的问题，清单文件配置 `windowSoftInputMode="adjustResize|stateHidden"`。
- 修复了正文阅读页底部进度信息的显示逻辑反向问题，重构为安静阅读时微光常驻，滑动或转动表冠时敏捷淡出避让，静止后平滑淡入恢复。
- 修复了阅读进度坐标域失真：排版正文为段落追加缩进后字符索引不再等于原文偏移，导致保存的阅读位置、全书百分比与章节定位混用两套坐标互相污染；新增 `ChapterOffsetMapper` 段落级双向精确映射，保存位置统一回归原文坐标域，恢复定位按原文坐标精确反解，根除进度百分比漂移与跨章节错位。
- 修复了阅读页常规模式下物理表冠旋转无振动与步进漂移：转冠统一接入表冠管线（固定单格步进 + 齿轮微振），不再落给原生视图裸滚动。
- 修复了书架主页物理表冠滚动管线缺失：复用原生焦点代理模式接入表冠滚动与触觉微振，与菜单页手感保持一致。
- 修复了阅读页弧形寻道叠加层吞掉整屏触摸的严重缺陷：全屏 `pointerInput` 与原生正文视图互操作时会拦截点击、长按与滑动，导致进入阅读页后无法翻页、无法呼出菜单、无法退出；重构为原生触摸管线驱动的 `ArcSeekGestureRecognizer` 纯状态机，叠加层降级为纯绘制组件。

### 优化
- 阅读页安静状态进度指示位置下移（底部 20dp → 10dp），更贴合阅读视线焦点。
- 统一应用内组件实现，能原生则原生：目录页浮层胶囊 `OverlayPill` 与 RSVP 底部自绘胶囊并入 UiKit `PillButton`（新增 Outline 主色描边强调级）；上下羽化渐隐遮罩四处内联实现收敛为 `EdgeFadeMask` 基元；`HairlineDivider`/`SurfaceCard`/`ProgressTrack` 底层改由原生 `Divider`/`OutlinedCard`/`LinearProgressIndicator` 承载（外观与性能语义不变）；无线传书环形传输进度由手绘 Canvas 双弧改用原生 `CircularProgressIndicator`。
- 优化了菜单设置界面的触控人机工效，所有卡片与按钮热区高度扩充至 42dp 以上，彻底杜绝小圆屏误触。
- 优化了菜单设置界面的物理表冠接入，支持全屏原生物理表冠滚动与细腻触觉微振。
- 优化了全应用物理表冠手感一致性：抽取 `normalizeToPixels` 统一档位归一化（单格固定 40px 步进），并以 `CrownTickGate` 距离门限节流齿轮微振——对齐 OPPO `crown_vibrator_instance` 的 24px 计振规则（原实现逐事件无脑触发，快转时振感粘连、慢速微小转动又乱振），书架、菜单、目录、阅读页四处手感统一。
- 优化了弧形章节标题绘制性能，使用 CharArray 就地绘制消灭临时字符串对象分配。
- 优化了设置菜单亮度预设按钮排布，重构为 2+3 双行自适应排布，消灭 5 联排时的文字折行破位并大幅扩展触控热区。
- 优化了书架删除按钮、置顶按钮与底栏字号增减按钮的物理触控热区，命中盒扩充至 40dp+，彻底根除点按删除时误触打开全书的隐患。
- 优化了全屏 GPU 过度绘制性能，剥离顶层 Compose Surface 与内层 ScrollView 重复底色，降低手表端 GPU 负载与发热耗电。
- 优化了触控微反馈体验，恢复全应用关键操作按钮的 Material 触控水波纹反馈。
- 优化了目录与章节列表视觉体系，重构为契合圆屏安全区的圆角卡片流，强化当前章节高亮与阅读状态标识。
- 优化了侧边时间与电量弧度绘制算法，重构为外轮廓相切对齐，消除字符宽度不均引起的弧度塌陷与视觉畸变。
- 优化了长章节正文排版性能，重构为就地索引拼接，大幅降低章节翻页与加载时的内存分配开销。
- 优化了配置持久化架构，彻底移除已废弃的历史遗留冗余逻辑，提升数据读写纯净度与稳定性。
- 优化了阅读器与菜单配置架构，引入 `ThemeMode`、`TapPageArea`、`FontType` 领域强类型枚举与 `TapPageHelper`、`ReadDurationFormatter` 纯函数模块，重构消除原始类型痴迷、重复 Switch 与伪测试用例。
- 优化了非阅读界面的整体视觉与动效体系，新建腕上设计系统 `UiKit`：统一动效令牌、发丝描边卡片、胶囊按钮、细进度轨、分区标签与错峰入场编排，书架、菜单、目录与无线传书界面全面接入，观感更沉稳高级。
- 优化了连续动效性能，全部动画值迁移至 `graphicsLayer` / 绘制阶段读取，逐帧失效不再触发重组，降低高刷滚动期 GC 抖动与功耗。
- 优化了页面切换体验，新增短促淡入与微缩放归位过渡，键控于屏幕类型，阅读进度等参数变化不触发过渡动画。
- 优化了目录界面顶部 Tab，升级为滑动式填充指示与文字颜色双通道动画；底部操作栏与关闭胶囊统一为按压回弹 + 轻触感胶囊按钮。
- 优化了目录与选卷列表滚动性能，缓存两级卡片背景绘制对象，消除快速滚动期逐帧 `GradientDrawable` 分配引发的 GC 抖动。
- 优化了无线传书界面质感，进度环描边收窄并柔化底轨，服务就绪状态点改为呼吸动效，大号传输百分比接入统一排版令牌。
- 优化了加载中界面，重构为旋转弧环 + 呼吸文字的轻量动效。
- 优化了书架主页信息层级，书籍卡片新增格式徽章、发丝描边与弹簧进度轨，删除两段式确认机制迁移至 Compose 列表行并保留超时自动回滚。
- 优化了书架主页代码纯净度，移除遗留原生 View 双实现死代码（约 630 行）。
- 优化了阅读页高频重组开销，根治滚动卡顿的三大来源：
  1. AndroidView update 阶段零对象分配：章节导航卡片背景 `GradientDrawable` 池化复用，仅主题色真正变化时重着色，不再每次重组新建并重设背景（此前滚动期间逐帧分配并双 View 失效）；
  2. 底部进度指示淡入淡出 alpha 迁移至 `graphicsLayer` 绘制阶段读取，整数百分比经 `derivedStateOf` 去重，滚动期间仅百分比数字真正变化才重组，杜绝逐帧全页重组；
  3. 羽化渐变 `Brush` 随主题缓存，滚动重组不再逐帧重建。
- 优化了弧形快速寻道的重组路径：三项渲染状态聚合为单一 `ArcSeekUiState` 容器，游标角度于叠加层 Canvas 绘制阶段延迟读取，悬浮胶囊文案经 `derivedStateOf` 去重，寻道全程 MOVE 事件不再引发阅读页重组。
- 优化了无线传书二维码生成时序，位图编码迁移至 Default 线程异步执行（`produceState`），入场首帧不再被同步编码阻塞掉帧，生成期间以同尺寸占位保持布局稳定。
- 优化了无线传书扫码界面圆屏适配：内容整体垂直居中并压缩纵向总高（96dp 二维码 + 主色扫码取景角标 + 等宽字体地址胶囊 + 呼吸状态行），二维码、地址与操作按钮全部落于 466px 圆屏黄金安全区，修复此前顶部锚定长列将底部操作挤出屏幕的问题；接收计数并入状态行，服务运行中收敛为单返回胶囊避免双胶囊贴边。
- 优化了书架列表过滤排序开销，结果随书架与搜索词 `remember` 缓存，搜索输入与删除确认重组不再反复全表扫描排序。
- 优化了弧形状态栏绘制纯净度，冒号下移修正量移出逐字符循环预计算。

### 其他
- 开源许可协议更新为 CC BY-NC-SA 4.0，明确禁止任何形式的商业用途与收费分发。

## [v1.1.0] - 2026-08-19

### 新增
- 添加了专为手表屏幕定制的极简腕表阅读主题应用图标与自适应圆形图标。
- 添加了局域网 Wi-Fi 网页无线传书功能，手机或电脑浏览器输入 IP 网址即可批量拖拽上传 TXT 与 EPUB 电子书，支持实时传输进度指示与上传完成即时入库书架。
- 添加了 RSVP 动态单行速读（闪读模式），表盘中央大字号呈现词元流并按标点智能调节节奏，全面支持物理表冠旋转调速与触屏档位切换。
- 添加了书签管理功能，快捷菜单一键标记当前阅读段落，目录页支持“目录 / 书签”双 Tab 切换与秒级定位跳转。
- 添加了书架即时搜索筛选与书籍置顶/取消置顶管理功能。
- 添加了 EPUB（.epub）电子书格式解析与阅读支持，兼容 EPUB 2（NCX）、EPUB 3（HTML5 Nav）与 Spine 线性回退多级目录抽取。
- 书架卡片添加了 TXT 与 EPUB 格式胶囊标识及书名自动净化。

### 优化
- 优化了 EPUB 加载性能，重构为随机访问与单趟流式索引双引擎架构，消除全量重复解压开销，大文件与目录实现毫秒级秒开。
- 优化了章节切换流畅度，引入章节正文内存 LRU 高速缓存池，前后翻页实现即时直出。
- 优化了物理表冠调速手感，引入自适应刻度累加器与防抖滤波算法，调速稳健扎实，杜绝灵敏度过高与数值狂飙。
- 优化了无线传书与闪读界面的视觉布局，采用纯粹克制 AMOLED 原生排版，操作栏抬升至安全区，彻底消除圆形屏幕边缘裁切。
- 优化了单一 XHTML 内部多章节锚点区间切片算法，避免长章节内容重复展示。

### 修复
- 修复了无线传书完成后退出界面未能即时刷新书架列表的问题。
- 修复了闪读模式下物理表冠旋转事件被顶层分发管线拦截的问题。
- 修复了阅读器首章标题与顶部弧形章节头部的重叠避让问题。

## [v1.0.0] - 2026-08-19

### 新增
- 支持本地 TXT 电子书导入与多书籍书架管理。
- 支持圆屏内接安全区适配，消除边缘弧形裁切。
- 支持物理旋转表冠平滑滚屏与线性马达触觉反馈。
- 支持多档速度的自动平滑滚屏引擎。
- 支持 GBK 与 UTF-8 编码自动识别及智能章节索引。
- 支持断点记忆、阅读进度持久化与快速跳章。
- 支持字体大小无级调节、AMOLED 纯黑省电模式与独立亮度控制。
